name: "servicechain-actions"
on:
   schedule:
     - cron: '0,10 * * * *'
   workflow_dispatch:
      inputs:
         logLevel:
            description: 'Log level'     
            required: true
            default: 'warning'
         tags:
            description: 'Test scenario tags'
jobs:
  docker-compose:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Start containers
      run: docker-compose -f "docker-compose-servicechain.yml" up -d

    - name: Do Some Test
      run: echo $GITHUB_SHA
      
    - name: Check Status contrainers
      run: |
        docker exec -it servicechain-actions-EN-0-1 cat /klaytn/log/init.log
        docker exec -it servicechain-actions-SCN-0-1 cat /klaytn/log/init.log

    - name: Stop containers
      if: always()
      run: docker-compose -f "docker-compose-servicechain.yml" down
